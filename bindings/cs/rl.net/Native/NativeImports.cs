namespace Rl.Net.Native {
   internal static class NativeImports {
      // NOTE: RLNETNATIVE for debug and release are the same,
      //       but this is a placeholder for future changes.
      #if DEBUG
      internal const string RLNETNATIVE = "rlnetnative";
      #else
      internal const string RLNETNATIVE = "rlnetnative";
      #endif
   }
}
